#include "pngFile.h"

namespace LTE
{
    pngFile::pngFile(const std::string& filePath): file(filePath){}

    pngFile::~pngFile(){}
    asset *pngFile::genrateAssetForWindow(windowPieceId)
    {
        return nullptr;
    }

    void pngFile::loadFileData()
    {

    }
}